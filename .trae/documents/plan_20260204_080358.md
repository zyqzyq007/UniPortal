I will update `src/pages/projects/ProjectHome.vue` to add a CSS rule for `.right-column`.

Currently, the `.right-column` container lacks layout properties, causing the "Editor" and "Task Status" cards to stack without spacing.

I will add the following style to match the `.left-column` spacing:
```css
.right-column {
  display: flex;
  flex-direction: column;
  gap: 24px;
}
```
This will ensure a consistent 24px gap between the cards.