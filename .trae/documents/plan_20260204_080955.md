I will implement the "Personal Center" (User Profile) page and link it to the user span in the top bar.

### Plan:

1.  **Update `src/utils/auth.ts`**:
    - Add `getUserAvatar`, `setUserAvatar`, `getUserEmail`, `setUserEmail` helper functions to persist user data in `localStorage`.

2.  **Create `src/pages/user/UserProfile.vue`**:
    - **Layout**: A page with a top navigation bar (consistent with the app style) and a central content area.
    - **Content**:
      - **Avatar**: A circular image. Clicking it triggers a file upload to change the avatar.
      - **Username**: Read-only display.
      - **Email**: Editable input field.
      - **Actions**:
        - "Change Password" button (mocks functionality).
        - "Switch Account" button (logs out and redirects to login).
        - "Logout" button.
    - **Logic**: Use the helpers from `auth.ts` to load and save data.

3.  **Update `src/router/index.ts`**:
    - Add a new route `/profile` pointing to `UserProfile.vue`.

4.  **Update `src/layouts/DefaultLayout.vue`**:
    - Wrap the username `span` in a `<RouterLink to="/profile">` or add a click handler to navigate to `/profile`.
    - Add CSS to indicate interactivity (cursor pointer, hover effect).