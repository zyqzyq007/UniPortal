I will update `src/pages/user/UserProfile.vue` to enhance the personal center functionalities as requested.

### Plan:

1.  **Create Modal Component**: I will implement a reusable modal (dialog) logic within `UserProfile.vue` (or a separate component if complexity grows, but inline is fine for now) to handle:
    -   **Password Change Flow**:
        -   Step 1: Modal asks for "Original Password".
        -   Step 2: Modal asks for "New Password" (only if step 1 is correct).
        -   Validation: Check original password (mock check) and show error if incorrect.
    -   **Switch Account Confirmation**:
        -   Show a modal with "Confirm Switch Account" title and warning message.
        -   Buttons: "Confirm Switch" (logs out) and "Cancel".

2.  **Add Page-Level Buttons**:
    -   Add a footer section to the card or the page content.
    -   **"Return" Button**: `router.back()` or link to previous page.
    -   **"Save" Button**: To explicitly save the email (and potentially other future fields). I will refactor the existing "Save" button next to the email input to be a page-level action, or keep both but clarify their roles. The request asks for a "Save" button at the bottom. I will move the primary save action to the bottom.

3.  **Refactor `UserProfile.vue`**:
    -   **Template**:
        -   Add `<div class="dialog-overlay" v-if="showDialog">...</div>` for the modals.
        -   Add the bottom button bar with "Return" and "Save".
    -   **Script**:
        -   State variables for dialogs (`showPasswordDialog`, `passwordStep`, `passwordInput`, `showSwitchDialog`).
        -   Functions for handling password flow (`verifyOldPassword`, `saveNewPassword`).
        -   Functions for page actions (`goBack`, `saveProfile`).

### Technical Details:
-   **Dialogs**: CSS-based modals with `fixed` positioning and `z-index`.
-   **Transitions**: Use Vue's `<Transition>` for smooth fade-in/out.
-   **Validation**: Simple check (e.g., `if (input === 'mock_password')`).

### File Changes:
-   `src/pages/user/UserProfile.vue`: Heavy modifications to template, script, and style.