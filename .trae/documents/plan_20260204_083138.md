I will add a "Back" button to the `ToolsCenter` page that navigates directly to the Project Overview page.

### Plan:
1.  **Modify `src/pages/tools/ToolsCenter.vue`**:
    -   Add a "Back" button to the top-left of the page, similar to the one implemented in `ProjectsList.vue`.
    -   **Functionality**: Instead of `router.back()`, it will use `router.push(\`/projects/${projectId}\`)` to ensure it goes to the Project Overview, meeting the requirement "directly jump to Project Overview page, not browser history previous page".
    -   **Styling**: Use the same `.btn-back` style (SVG icon + text) as seen in `ProjectsList.vue` to maintain consistency.

### Steps:
1.  Update the `<template>` in `ToolsCenter.vue` to include a header section with the back button and title.
2.  Update the `<script>` to import `useRouter` and implement the `goBack` function.
3.  Add the necessary CSS for the button styling (copying from `ProjectsList.vue` for consistency).

### Verification:
-   Check that clicking the button goes to `/projects/:projectId`.
-   Verify styles match other back buttons.
-   Verify browser history is preserved (pushing a new state, not replacing, or as per standard navigation). *Correction*: "Directly jump" usually means `push`. Browser back/forward will still work naturally.

### File Changes:
-   `src/pages/tools/ToolsCenter.vue`