I will remove the "Back" button from the `ToolsCenter` page.

### Plan:
1.  **Modify `src/pages/tools/ToolsCenter.vue`**:
    -   Remove the "Back" button HTML from the `<template>`.
    -   Remove the `goBack` function and `useRouter` import (if unused) from the `<script>`.
    -   Remove the CSS styles related to the back button (`.btn-back`, `.left-actions`, responsive media query) from `<style>`.
    -   Restore the simple header layout: `<h2>工具中心</h2>` inside `.page-title`.

### File Changes:
-   `src/pages/tools/ToolsCenter.vue`