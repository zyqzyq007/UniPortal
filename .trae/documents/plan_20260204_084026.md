I will modify the "Return to Project List" link in `UserProfile.vue` to act as a "Back" button with unsaved changes detection.

### Plan:

1.  **Modify `src/pages/user/UserProfile.vue`**:
    -   **Change Link to Button**: Replace `<RouterLink to="/projects">` with a `<button @click="handleGoBack">`.
    -   **Track Previous Route**:
        -   Import `useRoute` and `beforeRouteEnter` (or use a router history approach, but simple state is easier).
        -   Since `beforeRouteEnter` is harder with `<script setup>`, I'll assume the user likely came from *some* page. The request says "return to the specific page before clicking 'Personal Center'". `router.back()` does exactly this.
    -   **Unsaved Changes Logic**:
        -   Update `handleGoBack` (and the existing `goBack` function which is currently used by the bottom "Return" button) to check `isChanged` or `avatar` changes.
        -   Wait, `isChanged` only tracks email. Avatar changes are immediate (upload & save). The prompt says "monitor if user modified any info".
        -   If `isChanged` is true:
            -   Prevent default navigation.
            -   Show a new dialog type `'unsaved'`.
            -   Dialog content: "You have unsaved changes, do you want to save?"
            -   Buttons: "Cancel Save" (discard & leave), "Save" (save & leave).
    -   **Navigation**:
        -   If no changes: `router.back()`.
        -   If "Cancel Save": Reset state (optional) -> `router.back()`.
        -   If "Save": Call `saveProfile()` -> `router.back()`.

2.  **Implementation Details**:
    -   Add `currentDialog.value = 'unsaved'` logic.
    -   Add `discardAndLeave` and `saveAndLeave` functions.
    -   Update the top bar "Return" link to use this logic.

### File Changes:
-   `src/pages/user/UserProfile.vue`: Update template (top bar link -> button), add script logic for unsaved changes guard.