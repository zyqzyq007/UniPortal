I will optimize the scroll interaction design of the "Project Overview" page (`src/pages/projects/ProjectHome.vue`).

### Plan:

1.  **Modify `src/pages/projects/ProjectHome.vue`**:
    -   **Add Scroll Indicator**:
        -   Add a floating arrow indicator (`↓`) at the bottom of the page that bounces to suggest scrolling.
        -   Logic: Only show when the content height exceeds the viewport height and the user hasn't scrolled to the bottom.
        -   Hide it when scrolling down.
    -   **Gradient Mask**:
        -   Add a `::after` pseudo-element or a fixed overlay at the bottom to create a subtle fade-out effect for content, reinforcing that there is more below.
    -   **CSS Improvements**:
        -   Ensure `scroll-behavior: smooth` is set for the main container.
        -   Use `webkit-overflow-scrolling: touch` for iOS inertia scrolling.
    -   **Script Logic**:
        -   Use a `ScrollObserver` (or simple `scroll` event listener) to toggle the visibility of the scroll indicator.

### Implementation Details:
-   **Indicator**: Absolute positioned div at `bottom: 20px`.
-   **Animation**: CSS `@keyframes bounce`.
-   **Detection**: `onMounted` check `scrollHeight > clientHeight`. Listen to `scroll` event to hide indicator.

### File Changes:
-   `src/pages/projects/ProjectHome.vue`