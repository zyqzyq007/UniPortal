I will optimize the "Project Overview" page (`src/pages/projects/ProjectHome.vue`) to improve the scroll experience and provide clear visual cues for scrollability.

### Implementation Plan:

1.  **Structural Update**:
    -   Wrap the dashboard content in a dedicated `.dashboard-container` that handles vertical scrolling.
    -   Implement a fixed-position bottom bar to host the scroll indicator and gradient mask.

2.  **Visual Hints**:
    -   **Scroll Indicator**: Add a floating, bouncing arrow indicator (`↓`) at the bottom center. It will disappear as the user scrolls down and reappear if they scroll back up and content remains below.
    -   **Gradient Mask**: Add a subtle white-to-transparent gradient overlay at the bottom of the viewport to create a "content truncation" effect.

3.  **Interaction & Feedback**:
    -   **Elastic & Inertia Scrolling**: Use CSS properties like `-webkit-overflow-scrolling: touch` and `scroll-behavior: smooth`.
    -   **Dynamic Feedback**: Add a subtle parallax-like effect where the header or main cards slightly shift vertically as the user scrolls, providing tactile feedback.

4.  **Responsive Logic**:
    -   Use `onMounted` and `window.resize` listeners to check if the content actually overflows.
    -   Dynamically toggle the visibility of scroll hints based on the scroll position and container height.

5.  **Performance**:
    -   Use CSS transforms and transitions for animations to ensure 60fps performance.
    -   Debounce or throttle scroll event listeners to minimize main-thread impact.

### Technical Steps:
-   Modify `ProjectHome.vue` to include the new wrapper and overlay elements.
-   Add comprehensive CSS for animations, masks, and scroll behaviors.
-   Implement Vue 3 Composition API logic to handle scroll state management.