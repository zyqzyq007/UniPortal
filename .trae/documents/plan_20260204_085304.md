I will fix the truncated blue border issue on the active file tree node.

The issue is likely caused by the `outline` property in `.row.active`. `outline` is drawn outside the element's border box, and if the container (`.file-tree-card` or `.tree-container`) has `overflow: hidden` or tight padding, the outline can get clipped.

### Plan:
1.  **Modify `src/components/TreeNode.vue`**:
    -   Change `.row.active` to use `border` instead of `outline` to ensure the border is calculated within the layout (though `border` takes up space, which might shift content).
    -   Alternatively, keep `outline` but add `outline-offset: -1px` to draw it inside.
    -   Or better, use `box-shadow` which is generally safer for "borders" that shouldn't affect layout but should be visible.
    -   Given the user said "blue box border is swallowed/truncated", using `border: 1px solid #bfdbfe` is the most robust fix if I adjust padding to compensate (or just accept 1px shift).
    -   Actually, looking at the code:
        ```css
        .row.active {
          background: #eff6ff;
          outline: 1px solid #bfdbfe;
        }
        ```
    -   I will switch to `border: 1px solid #bfdbfe;` and reduce padding by 1px to prevent layout shift, OR just use `box-shadow: inset 0 0 0 1px #bfdbfe;` which is the modern way to do "inner borders" without layout shifts.

### Selected Approach:
Use `box-shadow: inset 0 0 0 1px #bfdbfe;` for `.row.active`. This ensures the "border" is inside the element and won't be clipped by parent overflow settings.

### File Changes:
-   `src/components/TreeNode.vue`