I will enhance the login page to include a user registration option as requested.

### Plan:

1.  **Modify `src/pages/auth/Login.vue`**:
    -   **Add Registration Section**: Insert a "register-section" below the login button.
    -   **Text & Link**: Add text "Don't have an account?" followed by a "Register Now" button/link.
    -   **State Management**: Add `isRegister` state to toggle between Login and Register forms.
    -   **Register Form**:
        -   Duplicate the login form structure but add "Confirm Password" and maybe "Email" fields for realism (or keep it simple as per "account/password" for now, but usually register needs confirmation). I'll stick to a simple toggle that changes the form title and button text, and adds a "Confirm Password" field.
    -   **Validation**: Add basic validation for the registration fields (e.g., passwords match).
    -   **Mock Registration**: Implement `handleRegister` which saves the user (using `auth.ts` logic if appropriate, or just mock success) and switches back to login or auto-logins.

2.  **Visual Design**:
    -   Style the toggle link to be distinct (e.g., blue text, cursor pointer).
    -   Ensure the card height adjusts smoothly or is sufficient.
    -   Mobile responsiveness: The existing card layout is responsive, just ensure the new link has enough touch padding.

### File Changes:
-   `src/pages/auth/Login.vue`: Major update to support two modes (Login/Register).

### Details:
-   **Toggle Logic**: `const isRegister = ref(false)`
-   **Form Fields**:
    -   Login: Account, Password
    -   Register: Account, Password, Confirm Password
-   **Actions**:
    -   "Login" button -> `handleLogin`
    -   "Register" button -> `handleRegister`
-   **Switch**: "No account? Register" vs "Have account? Login" at the bottom.

This approach keeps everything in one file for simplicity unless the user explicitly asked for a separate page. The request says "click register option should jump to register page OR expand register form". Expanding/Toggling in place is smoother for SPA. I will use the "Toggle/Expand" approach within `Login.vue` to keep it simple and seamless.