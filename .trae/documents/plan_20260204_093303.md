I will adjust the card layout in `ProjectsList.vue` to ensure exactly 4 cards per row on standard screens while maintaining responsiveness.

### Plan:

1.  **Layout Logic**:
    -   Container max-width: `1200px` (or full width with padding).
    -   Desired: 4 cards per row.
    -   Gap: `12px` (as suggested 8-12px).
    -   Card width calculation: `calc((100% - 3 * 12px) / 4)`.
    -   Wait, previously we used fixed `320px` width. The new requirement says "Calculate and set each card width to 25% of container width minus gap". So fixed `320px` must be removed and replaced with fluid width.

2.  **CSS Updates**:
    -   Update `.card-grid`:
        -   `display: flex; flex-wrap: wrap; gap: 12px;`
    -   Update `.card`:
        -   `width: calc((100% - 36px) / 4);` (where 36px is 3 gaps * 12px).
        -   `min-width: 240px;` (to prevent too narrow cards).
        -   Height: Keep fixed `240px` (or flexible, but user said "ensure content doesn't truncate", previously fixed height was requested). Point 6 says "ensure content doesn't truncate or deform". I'll keep fixed height for consistency but ensure internal scrolling or clamping works.

3.  **Responsiveness**:
    -   **Standard (Desktop > 1200px)**: 4 cards/row.
    -   **Medium (Tablet > 768px)**: 3 cards/row? Or 2? The user says "add media query for extreme small screens (consider 2x2)".
    -   I'll implement breakpoints:
        -   `@media (max-width: 1024px)`: 3 cards (`calc((100% - 24px) / 3)`).
        -   `@media (max-width: 768px)`: 2 cards (`calc((100% - 12px) / 2)`).
        -   `@media (max-width: 480px)`: 1 card (`100%`).

4.  **Verification**:
    -   Check Chrome/Firefox/Safari compatibility (Flexbox calc is widely supported).
    -   Verify card content safety.

### File Changes:
-   `src/pages/projects/ProjectsList.vue`: Update CSS for `.card` width and media queries.