I will modify the frontend component to enable direct navigation to the external tool URLs.

### Changes to be made:
1.  **Modify `src/pages/tools/ToolsCenter.vue`**:
    *   Update the "进入工具" (Enter Tool) button logic.
    *   Replace the existing `RouterLink` (which navigates to an internal route) with a standard `<a>` tag.
    *   Bind the `href` attribute to the `tool.targetUrl` field from the data.
    *   Add `target="_blank"` to open the tools in a new tab (preserving the portal page).

2.  **Configuration**:
    *   The tool URLs are currently read from `src/mock/data.ts`.
    *   The system will use the `targetUrl` field (e.g., `https://example.com/doc-review`) defined in that file.
    *   **Note**: You will need to update `src/mock/data.ts` with the actual deployed URLs for your 6 tools.

### Implementation details:
```html
<!-- Before -->
<RouterLink class="btn" :to="`/projects/${projectId}/tools/${tool.key}`">
  进入工具
</RouterLink>

<!-- After -->
<a 
  class="btn" 
  :href="tool.targetUrl" 
  target="_blank"
  rel="noopener noreferrer"
>
  进入工具
</a>
```
