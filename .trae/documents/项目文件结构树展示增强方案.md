## 现状分析
- 当前文件树组件 [FileTree.vue](file:///Users/bytedance/kairos/tasks/UniPortal/src/components/FileTree.vue) 已具备搜索框与基础树渲染，但“展开/收起”“全部展开/收起”“层级引导线”“动画过渡”不完整。
- [TreeNode.vue](file:///Users/bytedance/kairos/tasks/UniPortal/src/components/TreeNode.vue) 支持节点递归与折叠，但没有统一的“展开/收起全部”控制，也没有最大深度控制与明显引导线。

## 改造目标
实现标准树形目录展示，支持多级嵌套、展开/收起、搜索定位、全局展开/收起按钮、平滑动画与响应式优化。

## 计划与改动点
### 1) 组件功能增强
- 在 [FileTree.vue](file:///Users/bytedance/kairos/tasks/UniPortal/src/components/FileTree.vue) 增加“全部展开/收起”按钮。
- 新增状态 `expandAll`，通过 props 传递给 TreeNode，控制默认展开状态。
- 搜索功能保留，并在过滤结果中自动展开匹配路径。

### 2) TreeNode 展开逻辑与层级控制
- 在 [TreeNode.vue](file:///Users/bytedance/kairos/tasks/UniPortal/src/components/TreeNode.vue) 增加：
  - `level` 层级参数，限制最大 5 层深度。
  - `expandAll` 参数：当用户点击“全部展开/收起”时统一展开/折叠。
  - 折叠/展开动画（Vue `<Transition>` 结合高度/透明度）。

### 3) 视觉层级表现
- 增加清晰的缩进与引导线（左侧竖线 + 节点连线），保持现有图标与文件名样式。
- 对移动端调整缩进、图标大小与间距，保证可读性。

### 4) 交互优化
- 文件夹点击仅控制展开/收起，文件点击维持原功能。
- 展开/收起时使用轻量动画过渡。
- 搜索时自动展开命中路径，提升定位效率。

### 5) 验证与测试
- 验证 1~5 层目录显示与展开。
- 验证搜索命中与展开逻辑。
- 验证响应式布局（小屏滚动与可读性）。
- 前端 build 通过。

## 实施步骤
1) 修改 FileTree.vue：添加全局展开/收起按钮、expandAll 状态、搜索联动。
2) 修改 TreeNode.vue：支持 level、expandAll、动画与视觉引导线。
3) 样式调整：缩进、引导线、响应式。
4) 构建与验证。