{
  "name": "rag-platform",
  "version": "1.0.0",
  "private": true,
  "description": "Domain-adaptive RAG + agent platform (Python backend + Vue frontend).",
  "workspaces": [
    "web"
  ],
  "scripts": {
    "build": "npm run build --workspace web",
    "test:e2e": "playwright test --config=web/playwright.config.ts",
    "playwright:install": "playwright install --with-deps chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.47.0",
    "playwright": "^1.47.0"
  }
}
